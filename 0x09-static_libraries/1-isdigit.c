#include "holberton.h"

/**
 * _isdigit - Entry point
 * @c: integer
 * Return: 1 if is digit
 * 0 if is otherwise
 */
int _isdigit(int c)
{
	if (c >= 48 && c <= 57)
	{
		return (1);
	}
	else
	{
		return (0);
	}
return (0);
}
